# Day11_JavaIgnite2026

1)What is the difference between:

Checked Exception : shows Run time errors

Unchecked Exception : During compile time errors

Give one Java example for each.
Checked Exception → IOException, SQLException, FileNotFoundException
Unchecked Exception → ArithmeticException, NullPointerException


2)Explain difference between:

throw : actually create and send an exception

throws : a method signature to tell the compiler

Where do we use each one?
we can use both or either one of them


3)Why do we need it instead of built-in exceptions?

Give a real-life example where custom exception is useful.
throw : I am throwing the ball.
throws :This method may throw a ball.
